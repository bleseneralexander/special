titleDirective:UKRAINE(slight);
function():["TRIGGER"]*
  TRIGGER():point.["battery":"wire"];
TRIGGER(function(charge++));
charge == "battery" + "wire";
charge++ + charge++ + charge++;
function(charge[0]);
charge[1000];
point.charge();
